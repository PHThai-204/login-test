# login-test

# Instruct:

1) Execute command: `flutter pub get`
2) Execute command: `flutter pub run build_runner build --delete-conflicting-outputs` to gen assets
   and dependencies
3) Execute command: `flutter run` to run app

# Data test

- taxCode:  020076664492
- username: test
- password: 1234567
